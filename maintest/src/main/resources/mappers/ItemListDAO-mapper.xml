<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.ItemListDAO-mapper">
	<!-- 아이템 카테고리 전체 조회 -->
	<select id="selectAllCategory"
		resultType="ItemCategoryVO">
		select ca_num, ca_name
		from i_category
	</select>

	<!-- 선택한 카테고리의 아이템 전체 조회 불러오기x-->
	<select id="selectAllItem"
		resultType="ItemVO">
		select t2.ca_num as "itemCategoryVO.ca_num",
		t2.ca_name as "itemCategoryVO.ca_name",
		t1.i_num,
		t1.i_name,
		t1.i_description
		from item t1, i_category t2
		where t1.ca_num = t2.ca_num
		<![CDATA[and t1.i_num <1000]]>
	</select>
	
	<!-- 사용자의 리스트 목록 불러오기 -->
	<select id="loadItemList" parameterType="String" resultType="ListVO">
	select 
		t1.l_num,
		t1.ucode as "memberVO.ucode",
		t1.l_name,
		t1.i_list,
		t1.cdate
		from list t1, member t2
		where t1.ucode = t2.ucode
		and t2.ucode = #{ucode}
	
	</select>

		<!-- 사용자의 리스트 이름만 불러오기 -->
	<select id="loadList" parameterType="String" resultType="ListVO">
    select 
		distinct t1.lnum,
		t1.ucode as "memberVO.ucode",
		t1.lname
		from list t1, member t2
		where t1.ucode = t2.ucode
		and t2.ucode = #{ucode}
	</select>
	
	<!-- 리스트 저장 : 이름 생성하기 -->
	<insert id="listNameInsert" parameterType="Map">
		 insert into list(lnum, ucode, lname)
    values(lnum_seq.nextval, #{ucode}, #{lname})	
	</insert>
	
	<!-- 이름이 저장된 리스트 번호 가져오기 -->
	<select id="getNum" parameterType="Map" resultType="int">
		select 
				lnum  
    from list
    where ucode=#{ucode}
		and lname=#{lname}
	</select>
	
	<!-- list에 저장할때  -->
	<!-- 기존 listing 에있는 아이템들 삭제-->
	<delete id="deleteListing" parameterType="long">
	delete from listing where lnum = #{lnum}
	</delete>
	<!-- mapper test용 -->
	<select id="countListing" parameterType="long" resultType="int">
	select count(*)
	from listing
	where lnum = #{lnum}
	</select>
	<!-- new item 저장 >> inum 반환-->
	<insert id="saveNewItem" parameterType="ItemVO">
		INSERT INTO ITEM(ca_num, i_num, i_name, lnum)
		VALUES(#{itemCategoryVO.ca_num},I_NUM2_SEQ.nextval,#{i_name},#{lnum})
		<selectKey keyProperty="i_num" resultType="long"
			order="AFTER">
			select I_NUM2_SEQ.currval
			from dual
		</selectKey>
	</insert>

<!-- 아이템 정보 insert -->
<insert id="insertListing">
insert into listing(linum, lnum, i_num, lname, checked, icount)
values(list_num_seq.nextval, #{lnum}, #{i_num}, #{lname}, #{checked}, #{icount})
</insert>

</mapper>
